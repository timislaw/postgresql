-- My solution
SELECT *
FROM people
FETCH FIRST 10 ROW ONLY;
-- other sols

SELECT *
FROM people
ORDER BY pid
LIMIT 10
OFFSET 0;

SELECT *
FROM people
ORDER BY pid
LIMIT 10
OFFSET 10;

SELECT *
FROM people
ORDER BY pid
LIMIT 10
OFFSET 20;

SELECT *
FROM people
ORDER BY pid
LIMIT 10
OFFSET 30;

SELECT *
FROM people
ORDER BY pid
OFFSET 0
FETCH NEXT 10 ROW ONLY;

SELECT *
FROM people
ORDER BY pid
OFFSET 10
FETCH NEXT 10 ROW ONLY;

SELECT *
FROM people
ORDER BY pid
OFFSET 20
FETCH NEXT 10 ROW ONLY;

SELECT *
FROM people
ORDER BY pid
OFFSET 30
FETCH NEXT 10 ROW ONLY;